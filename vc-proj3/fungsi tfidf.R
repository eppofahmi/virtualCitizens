# membuat fungsi tf-idf dari data frame 

fungsi_tfidf <- function(data, kolom1, kolom2, plot = T) {
  
  
}

media_1 <- fungsi_tfidf(data = media2014, 
                        kolom1 = clean_text, 
                        kolom1 = media)
